export const metadata = {
  title: 'Search Results - iBox SkySearch',
  description: 'Compare flights and book cheap flights for your trip.',
};

export default function FlightsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
